[package]
name = "tauri-mcp"
version = "0.0.2"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Tauri MCP plugin — AI-driven E2E testing tools (dev-only)"

[dependencies]
tauri = { version = "2", features = ["devtools"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
base64 = "0.22"